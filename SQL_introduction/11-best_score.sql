-- This SQL query selects the score and name columns from the second_table where the score is 10 or higher, and orders the results in descending order by score.
SELECT score, name FROM second_table WHERE score >= 10 ORDER BY score DESC;
