The ``1-my_list`` module
=========================

Using ``MyList``
----------------

### **Import function from module**  
    >>> MyList = __import__('1-my_list').MyList

### **Test with positive integers**  
    >>> my_list = MyList([2, 8, 3, 9])
    >>> my_list.print_sorted()
    [2, 3, 8, 9]

### **Test with negative integers**  
    >>> my_list = MyList([2, -3, 7, 1])
    >>> my_list.print_sorted()
    [-3, 1, 2, 7]

### **Test with an empty list**  
    >>> my_list = MyList([])
    >>> my_list.print_sorted()
    []

### **Test with duplicate numbers**  
    >>> my_list = MyList([5, 2, 8, 2, 5])
    >>> my_list.print_sorted()
    [2, 2, 5, 5, 8]

### **Test with an already sorted list**  
    >>> my_list = MyList([1, 2, 3, 4, 5])
    >>> my_list.print_sorted()
    [1, 2, 3, 4, 5]

### **Test with a reverse sorted list**  
    >>> my_list = MyList([10, 9, 8, 7])
    >>> my_list.print_sorted()
    [7, 8, 9, 10]

### **Test with all negative numbers**  
    >>> my_list = MyList([-5, -1, -10, -3])
    >>> my_list.print_sorted()
    [-10, -5, -3, -1]

### **Test with a mix of negative and positive numbers**  
    >>> my_list = MyList([-2, 4, -1, 0, 3])
    >>> my_list.print_sorted()
    [-2, -1, 0, 3, 4]

### **Test with a single element**  
    >>> my_list = MyList([42])
    >>> my_list.print_sorted()
    [42]

### **Test with very large and very small numbers**  
    >>> my_list = MyList([1000000, -1000000, 0, 999999, -999999])
    >>> my_list.print_sorted()
    [-1000000, -999999, 0, 999999, 1000000]

### **Test with repeated values**  
    >>> my_list = MyList([5, 5, 5, 5])
    >>> my_list.print_sorted()
    [5, 5, 5, 5]

### **Test with floats**  
    >>> my_list = MyList([2.5, 3.1, 1.8, 2.5])
    >>> my_list.print_sorted()
    [1.8, 2.5, 2.5, 3.1]

### **Test with mixed types (should raise a TypeError)**  
    >>> my_list = MyList([2, "a", 3, "b"])
    >>> my_list.print_sorted()
    Traceback (most recent call last):
        ...
    TypeError: '<' not supported between instances of 'str' and 'int'

### **Test with only zeros**  
    >>> my_list = MyList([0, 0, 0, 0])
    >>> my_list.print_sorted()
    [0, 0, 0, 0]

### **Test with one negative element**  
    >>> my_list = MyList([-42])
    >>> my_list.print_sorted()
    [-42]

### **Test with mixed integers and floats**  
    >>> my_list = MyList([3, 2.2, 1, 4.8, 2])
    >>> my_list.print_sorted()
    [1, 2, 2.2, 3, 4.8]

### **Test with None (should raise a TypeError)**  
    >>> my_list = MyList([1, None, 3])
    >>> my_list.print_sorted()
    Traceback (most recent call last):
        ...
    TypeError: '<' not supported between instances of 'NoneType' and 'int'

### **Test with boolean values (True and False are treated as 1 and 0)**  
    >>> my_list = MyList([True, False, 3, 1])
    >>> my_list.print_sorted()
    [False, True, 1, 3]

### **Test with a tuple (should raise a TypeError)**  
    >>> my_list = MyList([1, (2, 3), 4])
    >>> my_list.print_sorted()
    Traceback (most recent call last):
        ...
    TypeError: '<' not supported between instances of 'tuple' and 'int'

### **Test with a dictionary (should raise a TypeError)**  
    >>> my_list = MyList([1, {"key": 2}, 4])
    >>> my_list.print_sorted()
    Traceback (most recent call last):
        ...
    TypeError: '<' not supported between instances of 'dict' and 'int'

### **Test with extreme values (big and small numbers)**  
    >>> my_list = MyList([10**10, -10**10, 10**-10, -10**-10])
    >>> my_list.print_sorted()
    [-10000000000, -1e-10, 1e-10, 10000000000]
