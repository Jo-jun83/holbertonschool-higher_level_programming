The ``1-my_list`` module
=========================

Using ``MyList``
----------------

### **Import function from module**  
    >>> MyList = __import__('1-my_list').MyList

### **Test with positive integers**  
    >>> my_list = MyList([2, 8, 3, 9])
    >>> my_list.print_sorted()
    [2, 3, 8, 9]

### **Test with negative integers**  
    >>> my_list = MyList([2, -3, 7, 1])
    >>> my_list.print_sorted()
    [-3, 1, 2, 7]

### **Test with an empty list**  
    >>> my_list = MyList([])
    >>> my_list.print_sorted()
    []

### **Test with duplicate numbers**  
    >>> my_list = MyList([5, 2, 8, 2, 5])
    >>> my_list.print_sorted()
    [2, 2, 5, 5, 8]

### **Test with an already sorted list**  
    >>> my_list = MyList([1, 2, 3, 4, 5])
    >>> my_list.print_sorted()
    [1, 2, 3, 4, 5]

### **Test with a reverse sorted list**  
    >>> my_list = MyList([10, 9, 8, 7])
    >>> my_list.print_sorted()
    [7, 8, 9, 10]

### **Test with all negative numbers**  
    >>> my_list = MyList([-5, -1, -10, -3])
    >>> my_list.print_sorted()
    [-10, -5, -3, -1]

### **Test with a mix of negative and positive numbers**  
    >>> my_list = MyList([-2, 4, -1, 0, 3])
    >>> my_list.print_sorted()
    [-2, -1, 0, 3, 4]

### **Test with a single element**  
    >>> my_list = MyList([42])
    >>> my_list.print_sorted()
    [42]

### **Test with very large and very small numbers**  
    >>> my_list = MyList([1000000, -1000000, 0, 999999, -999999])
    >>> my_list.print_sorted()
    [-1000000, -999999, 0, 999999, 1000000]

### **Test with repeated values**  
    >>> my_list = MyList([5, 5, 5, 5])
    >>> my_list.print_sorted()
    [5, 5, 5, 5]

### **Test with floats (should work since `sorted()` supports them)**  
    >>> my_list = MyList([2.5, 3.1, 1.8, 2.5])
    >>> my_list.print_sorted()
    [1.8, 2.5, 2.5, 3.1]

### **Test with mixed types (should raise a TypeError)**  
    >>> my_list = MyList([2, "a", 3, "b"])
    >>> my_list.print_sorted()
    Traceback (most recent call last):
        ...
    TypeError: '<' not supported between instances of 'str' and 'int'
